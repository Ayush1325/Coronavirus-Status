///  Class for data of [info_item]

import 'package:flutter/material.dart';

class InfoData {
  final String title;
  final int num;
  final int delta;
  final Color color;

  InfoData(this.title, this.num, this.delta, this.color);
}
